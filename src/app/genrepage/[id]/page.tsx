import { SectionTwo } from "@/components/SectionTwo";

export default function HomePage({
  params: { id },
}: {
  params: { id: string };
}) {
  return <div>{/* <SectionTwo></SectionTwo> */}</div>;
}
